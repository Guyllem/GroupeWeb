<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StageConnect - Offres de {{ enterprise.Nom_Entreprise }}</title>
    <link rel="stylesheet" href="{{ asset('css/etudiant/main.css') }}">
</head>
<body>
<main>
    <div class="container">
        {% include 'partials/navbar-etudiant.html.twig' %}

        <div class="main-content">
            <div class="main-section">
                <!-- En-tête avec titre et info sur l'entreprise -->
                <div class="enterprise-header">
                    <h1 class="profile-title">Offres de {{ enterprise.Nom_Entreprise }}</h1>
                </div>

                <div class="search-bar">
                    <div class="back-button">
                        <a href="{{ url('entreprises/details/' ~ enterprise.Id_Entreprise) }}"><span class="arrow">←</span> Retour à l'entreprise</a>
                    </div>
                </div>

                {% if offers|length > 0 %}
                    {% for offer in offers %}
                        <div class="card">
                            <div class="card-header">
                                <h3>{{ offer.Titre_Offre }}</h3>
                                <div class="tags">
                                    {% for skill in offer.skills %}
                                        <span class="tag">{{ skill.Nom_Competence }}</span>
                                    {% endfor %}
                                </div>
                            </div>
                            <div class="card-body">
                                <p>{{ offer.Description_Offre }}</p>
                            </div>
                            <div class="card-footer">
                                <span class="publish-date">Publiée le {{ offer.Date_Debut_Offre|date('d/m/Y') }}</span>
                                <div class="actions">
                                    <a href="{{ url('offres/details/' ~ offer.Id_Offre) }}" class="view-btn">Regarder</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    <div class="no-applications">
                        <p>{{ enterprise.Nom_Entreprise }} n'a pas encore publié d'offres de stage.</p>
                        <a href="{{ url('entreprises') }}" class="view-btn">Découvrir d'autres entreprises</a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</main>
{% include 'partials/footer.html.twig' %}
<script src="{{ asset('js/frontend_script.js') }}"></script>
</body>
</html>