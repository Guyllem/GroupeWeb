=<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StageConnect - Réinitialisation du mot de passe</title>
    <link rel="stylesheet" href="{{ asset('css/pilote/main.css') }}">
    <link rel="stylesheet" href="{{ asset('css/utils/modal.css') }}">
</head>
<body>
{% set adminPage = true %}
{% include 'partials/navbar-pilote.html.twig' %}

<main>
    <div class="form-container">
        <a href="{{ url('admin/etudiants/' ~ student.Id_Etudiant) }}" class="btn-return">
            <span class="arrow">←</span> Retour
        </a>
        <h2 class="form-title">Réinitialisation du mot de passe</h2>

        <div class="student-info">
            <p>Vous êtes en train de modifier le mot de passe de <strong>{{ student.Prenom_Utilisateur }} {{ student.Nom_Utilisateur }}</strong></p>
        </div>

        <form id="reset-password-form" method="post" action="{{ url('admin/etudiants/' ~ student.Id_Etudiant ~ '/reset') }}" novalidate>
            <input type="hidden" name="csrf_token" value="{{ csrf_token|default('') }}">

            <div class="form-group">
                <label for="password" class="form-label">Nouveau mot de passe</label>
                <input type="password" id="password" name="password" class="form-input" required>
                <div id="password-error" class="form-error"></div>
            </div>

            <div class="form-group">
                <label for="confirm_password" class="form-label">Confirmer le mot de passe</label>
                <input type="password" id="confirm_password" name="confirm_password" class="form-input" required>
                <div id="confirm-password-error" class="form-error"></div>
            </div>

            <div class="password-requirements">
                <p>Le mot de passe doit :</p>
                <ul>
                    <li id="length-check">Contenir au moins 8 caractères</li>
                    <li id="match-check">Les deux mots de passe doivent correspondre</li>
                </ul>
            </div>

            <div class="form-actions">
                <a href="{{ url('admin/etudiants/' ~ student.Id_Etudiant) }}" class="btn btn-back">Annuler</a>
                <button type="submit" id="submit-btn" class="btn btn-submit" disabled>Réinitialiser le mot de passe</button>
            </div>
        </form>
    </div>
</main>

{% include 'partials/footer.html.twig' %}
<script src="{{ asset('js/reset_form.js') }}"></script>
<script src="{{ asset('js/navigation.js') }}"></script>
</body>
</html>