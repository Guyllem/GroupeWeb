<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StageConnect - Mes Offres</title>
    <link rel="stylesheet" href="{{ asset('css/etudiant/main.css') }}">
</head>
<body>
<main>
    <div class="container">
        {% include 'partials/navbar-etudiant.html.twig' %}

        <div class="main-content">
            <div class="main-section">
                <div class="search-bar">
                    <div class="back-button">
                        <a href="{{ url('etudiant') }}"><span class="arrow">←</span> Accueil</a>
                    </div>
                    <div class="search-container">
                        <input placeholder="Search..." class="search-input" name="search" type="search"/>
                    </div>
                </div>

                {% if offers|length > 0 %}
                    {% for offer in offers %}
                        <div class="card">
                            <div class="card-header">
                                <h3>{{ offer.Titre_Offre }}</h3>
                                <div class="tags">
                                    {% for skill in offer.skills %}
                                        <span class="tag">{{ skill.Nom_Competence}}</span>
                                    {% endfor %}
                                </div>
                            </div>
                            <div class="card-body">
                                <p>{{ offer.Description_Offre }}</p>
                            </div>
                            <div class="card-footer">
                                <span class="publish-date">Candidature du {{ offer.Date_Candidature|date('d/m/Y') }}</span>
                                <div class="actions">
                                    <a href="{{ url('offres/details/' ~ offer.Id_Offre) }}" class="view-btn">Regarder</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    <p class="main-title-wishlist">Vous n'avez postulé à aucune offre.</p>
                    <p class="sub-title-wishlist">C'est bien dommage ...</p>

                    <div class="back-design">
                        <img class="bulle2-1" src="{{ asset('assets/images/bulle2.png') }}" alt="bulle2-1" />
                        <img class="bulle2-2" src="{{ asset('assets/images/bulle2.png') }}" alt="bulle2-2" />
                        <img class="bulle3-1" src="{{ asset('assets/images/bulle3.png') }}" alt="bulle3-1" />
                        <img class="bulle3-2" src="{{ asset('assets/images/bulle3.png') }}" alt="bulle3-2" />
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</main>
{% include 'partials/footer.html.twig' %}
<script src="{{ asset('js/navigation.js') }}"></script>
<script src="{{ asset('js/wishlist.js') }}"></script>
</body>
</html>